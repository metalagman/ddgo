name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Module tidy check
        shell: bash
        run: |
          set -euo pipefail
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: Module verify
        run: go mod verify

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.10.1
          args: --timeout=5m

      - name: Unit tests
        run: go test ./...

      - name: Example tests
        run: go test ./... -run '^Example'

      - name: Race tests
        run: go test -race ./...

      - name: Compliance checks
        shell: bash
        run: |
          set -euo pipefail

          test -f LICENSE
          test -f THIRD_PARTY_NOTICES.md
          test -f licenses/LGPL-3.0-or-later.txt
          test -f compliance/provenance.json

          grep -q '"license": "LGPL-3.0-or-later"' compliance/provenance.json
          grep -q '"upstream_version": "' compliance/provenance.json

          compiled_sha=$(sha256sum sync/compiled.json | awk '{print $1}')
          manifest_sha=$(sha256sum sync/manifest.json | awk '{print $1}')

          grep -q "\"sha256\": \"${compiled_sha}\"" compliance/provenance.json
          grep -q "\"manifest_sha256\": \"${manifest_sha}\"" compliance/provenance.json
